<?xml version="1.0" encoding="utf-8" ?>
<Page
    x:Class="csc13001_plant_pos.View.RoleSelectionPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="using:csc13001_plant_pos.View"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}"
    mc:Ignorable="d">

    <Grid
        Grid.Row="1"
        HorizontalAlignment="Center"
        VerticalAlignment="Center">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <!--  Store Role Card  -->
        <Button
            x:Name="StoreRoleButton"
            Grid.Column="0"
            Width="200"
            Height="200"
            Margin="20"
            Background="White"
            Click="SaleButton_Click"
            CornerRadius="10">
            <StackPanel>
                <Image
                    Width="100"
                    Height="100"
                    Margin="0,20,0,10"
                    Source="/Assets/Icon/ShoppingIcon.png" />
                <TextBlock
                    HorizontalAlignment="Center"
                    FontSize="24"
                    FontWeight="SemiBold"
                    Text="Cửa hàng" />
            </StackPanel>
        </Button>

        <!--  Manager Role Card  -->
        <Button
            x:Name="ManagerRoleButton"
            Grid.Column="1"
            Width="200"
            Height="200"
            Margin="20"
            Background="White"
            Click="AdminButton_Click"
            CornerRadius="10">
            <StackPanel>
                <Image
                    Width="100"
                    Height="100"
                    Margin="0,20,0,10"
                    Source="/Assets/Icon/ManagerIcon.png" />
                <TextBlock
                    HorizontalAlignment="Center"
                    FontSize="24"
                    FontWeight="SemiBold"
                    Text="Quản lý" />
            </StackPanel>
        </Button>
    </Grid>
</Page>
