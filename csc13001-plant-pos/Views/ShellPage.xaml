<Page x:Class="csc13001_plant_pos.Views.ShellPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:helpers="using:csc13001_plant_pos.Helpers"
      xmlns:behaviors="using:csc13001_plant_pos.Behaviors"
      xmlns:i="using:Microsoft.Xaml.Interactivity"
      Loaded="OnLoaded">

    <Grid>
        <Grid x:Name="AppTitleBar"
              Canvas.ZIndex="1"
              Height="{Binding ElementName=NavigationViewControl, Path=CompactPaneLength}"
              IsHitTestVisible="True"
              VerticalAlignment="Top">
            <Image Source="/Assets/WindowIcon.ico"
                   HorizontalAlignment="Left"
                   Width="16"
                   Height="16" />
            <TextBlock x:Name="AppTitleBarText"
                       VerticalAlignment="Center"
                       TextWrapping="NoWrap"
                       Style="{StaticResource CaptionTextBlockStyle}"
                       Margin="28,0,0,0" />
        </Grid>
        <NavigationView x:Name="NavigationViewControl"
                        OpenPaneLength="100"
                        Canvas.ZIndex="0"
                        IsBackButtonVisible="Visible"
                        IsBackEnabled="{x:Bind ViewModel.IsBackEnabled, Mode=OneWay}"
                        SelectedItem="{x:Bind ViewModel.Selected, Mode=OneWay}"
                        IsSettingsVisible="True"
                        ExpandedModeThresholdWidth="1280"
                        DisplayModeChanged="NavigationViewControl_DisplayModeChanged"
                        ItemInvoked="NavigationView_ItemInvoked"
                        Header="{x:Bind ((ContentControl)ViewModel.Selected).Name, Mode=OneWay}">
            <NavigationView.MenuItems>
                <!--
                TODO: Update item titles by updating <x:Uid>.Content entries in Strings/en-us/Resources.resw.
                https://docs.microsoft.com/windows/uwp/app-resources/localize-strings-ui-manifest#refer-to-a-string-resource-identifier-from-xaml
            
                TODO: Update item icons by updating FontIcon.Glyph properties.
                https://docs.microsoft.com/windows/apps/design/style/segoe-fluent-icons-font#icon-list
                -->
                <!-- Staff -->
                <NavigationViewItem x:Name="Admin_Main"
                                    Margin="0,10,0,10"
                                    FontSize="12"
                                    helpers:NavigationHelper.NavigateTo="csc13001_plant_pos.ViewModels.MainViewModel">
                    <StackPanel Orientation="Vertical"
                                HorizontalAlignment="Center">
                        <FontIcon FontFamily="{StaticResource SymbolThemeFontFamily}"
                                  Glyph="&#xe7c3;" />
                        <TextBlock Text="Main"
                                   HorizontalAlignment="Center" />
                    </StackPanel>
                </NavigationViewItem>

                <NavigationViewItem x:Name="Staff_ListDetails"
                                    Margin="0,10,0,10"
                                    FontSize="12"
                                    helpers:NavigationHelper.NavigateTo="csc13001_plant_pos.ViewModels.ListDetailsViewModel">
                    <StackPanel Orientation="Vertical"
                                HorizontalAlignment="Center">
                        <FontIcon FontFamily="{StaticResource SymbolThemeFontFamily}"
                                  Glyph="&#xea37;" />
                        <TextBlock Text="List Details"
                                   HorizontalAlignment="Center" />
                    </StackPanel>
                </NavigationViewItem>

                <NavigationViewItem x:Name="Staff_ContentGrid"
                                    Margin="0,10,0,10"
                                    FontSize="12"
                                    helpers:NavigationHelper.NavigateTo="csc13001_plant_pos.ViewModels.ContentGridViewModel">
                    <StackPanel Orientation="Vertical"
                                HorizontalAlignment="Center">
                        <FontIcon FontFamily="{StaticResource SymbolThemeFontFamily}"
                                  Glyph="&#xf0e2;" />
                        <TextBlock Text="Content Grid"
                                   HorizontalAlignment="Center" />
                    </StackPanel>
                </NavigationViewItem>

                <NavigationViewItem x:Name="Admin_DataGrid"
                                    Margin="0,10,0,10"
                                    FontSize="12"
                                    helpers:NavigationHelper.NavigateTo="csc13001_plant_pos.ViewModels.DataGridViewModel">
                    <StackPanel Orientation="Vertical"
                                HorizontalAlignment="Center">
                        <FontIcon FontFamily="{StaticResource SymbolThemeFontFamily}"
                                  Glyph="&#xe80a;" />
                        <TextBlock Text="Data Grid"
                                   HorizontalAlignment="Center" />
                    </StackPanel>
                </NavigationViewItem>

                <!-- Admin -->
                <NavigationViewItem x:Name="Admin_Order"
                                    Margin="0,10,0,10"
                                    FontSize="12"
                                    helpers:NavigationHelper.NavigateTo="csc13001_plant_pos.ViewModels.OrderViewModel">
                    <StackPanel Orientation="Vertical"
                                HorizontalAlignment="Center">
                        <FontIcon FontFamily="{StaticResource SymbolThemeFontFamily}"
                                  Glyph="&#xE7BF;" />
                        <TextBlock Text="Order"
                                   HorizontalAlignment="Center" />
                    </StackPanel>
                </NavigationViewItem>

                <!-- Logout -->
                <NavigationViewItem x:Name="Logout"
                                    FontSize="12">
                    <StackPanel Orientation="Vertical"
                                HorizontalAlignment="Center">
                        <FontIcon FontFamily="{StaticResource SymbolThemeFontFamily}"
                                  Glyph="&#xF3B1;" />
                        <TextBlock Text="Logout"
                                   VerticalAlignment="Center" />
                    </StackPanel>
                </NavigationViewItem>
            </NavigationView.MenuItems>

            <NavigationView.HeaderTemplate>
                <DataTemplate>
                    <Grid>
                        <TextBlock Text="{Binding}"
                                   Style="{ThemeResource TitleTextBlockStyle}" />
                    </Grid>
                </DataTemplate>
            </NavigationView.HeaderTemplate>
            <i:Interaction.Behaviors>
                <behaviors:NavigationViewHeaderBehavior DefaultHeader="{x:Bind ((ContentControl)ViewModel.Selected).Name, Mode=OneWay}">
                    <behaviors:NavigationViewHeaderBehavior.DefaultHeaderTemplate>
                        <DataTemplate>
                            <Grid>
                                <TextBlock Text="{Binding}"
                                           Style="{ThemeResource TitleTextBlockStyle}" />
                            </Grid>
                        </DataTemplate>
                    </behaviors:NavigationViewHeaderBehavior.DefaultHeaderTemplate>
                </behaviors:NavigationViewHeaderBehavior>
            </i:Interaction.Behaviors>
            <Grid Margin="{StaticResource NavigationViewPageContentMargin}">
                <Frame x:Name="NavigationFrame" />
            </Grid>
        </NavigationView>
    </Grid>
</Page>
