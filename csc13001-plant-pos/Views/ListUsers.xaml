<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="csc13001_plant_pos.Views.ListUsers"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:csc13001_plant_pos.Views"
    xmlns:controls="using:CommunityToolkit.WinUI.UI.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid Padding="20" Width="800" Height="400">

        <StackPanel>
            <!-- Thanh tìm kiếm -->
            <TextBox x:Name="SearchBox" PlaceholderText="Tìm kiếm khách hàng..." Margin="0,0,0,10" TextChanged="SearchBox_TextChanged"/>

            <!-- Bộ lọc ngày -->
            <Grid VerticalAlignment="Center">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <!-- Bộ lọc ngày -->
                <CalendarDatePicker x:Name="DateFilter"
        PlaceholderText="Ngày đăng ký"
        DateChanged="DateFilter_DateChanged"
        Width="200"
        VerticalAlignment="Center"
        Grid.Column="0"/>

                <!-- Bộ lọc hạng -->
                <ComboBox x:Name="RankFilter" Width="150" PlaceholderText="Chọn hạng"
              SelectionChanged="RankFilter_SelectionChanged"
              VerticalAlignment="Center"
              Grid.Column="1" Margin="10,0,0,0">
                    <ComboBoxItem Content="Tất cả"/>
                    <ComboBoxItem Content="Đồng"/>
                    <ComboBoxItem Content="Bạc"/>
                    <ComboBoxItem Content="Vàng"/>
                    <ComboBoxItem Content="Bạch Kim"/>
                    <ComboBoxItem Content="Kim Cương"/>
                </ComboBox>

                <!-- Nút Reset -->
                <Button Content="Đặt lại" Click="ResetFilter_Click" Width="100" Background="Red" Foreground="White"
            VerticalAlignment="Center"
            Grid.Column="2" Margin="10,0,0,0"/>
            </Grid>




            <!-- Bọc DataGrid trong ScrollViewer để kích hoạt cuộn -->
            <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled" Height="300">
                <controls:DataGrid x:Name="CustomersGrid" ItemsSource="{x:Bind FilteredUsers, Mode=OneWay}" 
   AutoGenerateColumns="False" HeadersVisibility="Column" Margin="0,10,0,0" MinWidth="800">
                    <controls:DataGrid.Columns>
                        <controls:DataGridTextColumn Header="Id" Binding="{Binding ID}" Width="100"/>
                        <controls:DataGridTextColumn Header="Họ Tên" Binding="{Binding Name}" Width="*"/>
                        <controls:DataGridTextColumn Header="Điểm số" Binding="{Binding Points}" Width="120"/>
                        <controls:DataGridTemplateColumn Header="Hạng bậc thẻ" Width="150">
                            <controls:DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <Grid>
                                        <Border Padding="5" CornerRadius="10" BorderThickness="2"
                   BorderBrush="{Binding RankColor}"
                   Background="{Binding RankColor}"
                   Margin="5">
                                            <TextBlock Text="{Binding Rank}" FontWeight="Bold" Foreground="White"/>
                                        </Border>
                                    </Grid>
                                </DataTemplate>
                            </controls:DataGridTemplateColumn.CellTemplate>
                        </controls:DataGridTemplateColumn>
                        <controls:DataGridTextColumn Header="Ngày tạo" Binding="{Binding CreatedAt}" Width="150"/>
                    </controls:DataGrid.Columns>
                </controls:DataGrid>

            </ScrollViewer>


        </StackPanel>
    </Grid>
</Page>
