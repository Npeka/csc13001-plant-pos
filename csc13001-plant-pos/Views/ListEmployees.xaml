<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="csc13001_plant_pos.Views.ListEmployees"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:csc13001_plant_pos.Views"
    xmlns:controls="using:CommunityToolkit.WinUI.UI.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid Padding="24,20" MaxWidth="1200" HorizontalAlignment="Center">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Tiêu đề trang -->
        <TextBlock Text="Quản lý nhân viên" 
                   Grid.Row="0"
                   Style="{StaticResource TitleTextBlockStyle}" 
                   FontSize="24" 
                   FontWeight="SemiBold"
                   Margin="0,0,0,16"/>

        <!-- Panel chứa các điều khiển tìm kiếm và lọc -->
        <Grid Grid.Row="1" Margin="0,0,0,16">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Thanh tìm kiếm với icon -->
            <Grid Grid.Row="0" Margin="0,0,0,12">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBox x:Name="SearchBox" 
                         Grid.Column="0"
                         PlaceholderText="Tìm kiếm theo tên hoặc ID nhân viên..." 
                         TextChanged="SearchBox_TextChanged">
                    <TextBox.Resources>
                        <ResourceDictionary>
                            <ResourceDictionary.ThemeDictionaries>
                                <ResourceDictionary x:Key="Light">
                                    <SolidColorBrush x:Key="TextControlBackgroundFocused" Color="#F5F5F5"/>
                                </ResourceDictionary>
                            </ResourceDictionary.ThemeDictionaries>
                        </ResourceDictionary>
                    </TextBox.Resources>
                </TextBox>

                <Button Grid.Column="1" 
                        Margin="8,0,0,0"
                        Background="#0078D7"
                        Foreground="White"
                        CornerRadius="4">
                    <FontIcon FontFamily="Segoe MDL2 Assets" Glyph="&#xE721;"/>
                </Button>
            </Grid>

            <!-- Bộ lọc được bọc trong card -->
            <Grid Grid.Row="1" 
                  Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" 
                  BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                  BorderThickness="1"
                  CornerRadius="8"
                  Padding="16">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <!-- Bộ lọc ngày -->
                <StackPanel Grid.Column="0" Margin="0,0,8,0">
                    <TextBlock Text="Ngày bắt đầu làm việc" 
                               Margin="0,0,0,4" 
                               FontSize="12" 
                               Opacity="0.8"/>
                    <CalendarDatePicker x:Name="DateFilter"
                                        HorizontalAlignment="Stretch"
                                        DateChanged="DateFilter_DateChanged"/>
                </StackPanel>

                <!-- Bộ lọc vị trí -->
                <StackPanel Grid.Column="1" Margin="8,0,8,0">
                    <TextBlock Text="Vị trí làm việc" 
                               Margin="0,0,0,4" 
                               FontSize="12" 
                               Opacity="0.8"/>
                    <ComboBox x:Name="PositionFilter" 
                              HorizontalAlignment="Stretch"
                              SelectionChanged="PositionFilter_SelectionChanged">
                        <ComboBoxItem Content="Tất cả"/>
                        <ComboBoxItem Content="Quản lý"/>
                        <ComboBoxItem Content="Nhân viên bán hàng"/>
                        <ComboBoxItem Content="Nhân viên kho"/>
                        <ComboBoxItem Content="Kế toán"/>
                    </ComboBox>
                </StackPanel>

                <!-- Nút Reset -->
                <Button Grid.Column="2" 
                        Content="Đặt lại bộ lọc" 
                        Click="ResetFilter_Click" 
                        Margin="8,0,0,0"
                        VerticalAlignment="Bottom"
                        Background="Transparent"
                        BorderBrush="#D13438"
                        BorderThickness="1"
                        Foreground="#D13438"/>
            </Grid>
        </Grid>

        <!-- DataGrid với styling tốt hơn -->
        <Grid Grid.Row="2" 
              Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
              BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
              BorderThickness="1"
              CornerRadius="8">

            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Header cho DataGrid -->
            <Grid Grid.Row="0" 
                  Padding="16,12" 
                  BorderThickness="0,0,0,1" 
                  BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBlock Text="Danh sách nhân viên" 
                           FontWeight="SemiBold" 
                           VerticalAlignment="Center"/>

                <TextBlock x:Name="EmployeeCount" 
                           Grid.Column="1" 
                           Text="0 nhân viên" 
                           Opacity="0.7"
                           VerticalAlignment="Center"/>
            </Grid>

            <!-- DataGrid -->
            <controls:DataGrid x:Name="EmployeesGrid" 
                               Grid.Row="1"
                               ItemsSource="{Binding FilteredEmployees}" 
                               AutoGenerateColumns="False" 
                               HeadersVisibility="Column"
                               GridLinesVisibility="Horizontal"
                               CanUserReorderColumns="False"
                               Margin="0"
                               Padding="16">
                <controls:DataGrid.Resources>
                    <SolidColorBrush x:Key="DataGridColumnHeaderBackgroundColor" Color="Transparent"/>
                    <SolidColorBrush x:Key="DataGridColumnHeaderForegroundColor" Color="#666666"/>
                    <SolidColorBrush x:Key="DataGridGridLinesBrush" Color="#E0E0E0"/>
                    <Thickness x:Key="DataGridRowGroupHeaderMargin">0</Thickness>
                </controls:DataGrid.Resources>
                <controls:DataGrid.Columns>
                    <controls:DataGridTextColumn Header="ID" 
                                                Binding="{Binding ID}" 
                                                Width="70"/>
                    <controls:DataGridTextColumn Header="Họ Tên" 
                                                Binding="{Binding Name}" 
                                                Width="*"/>
                    <controls:DataGridTextColumn Header="Số điện thoại" 
                                                Binding="{Binding PhoneNumber}" 
                                                Width="150"/>
                    <controls:DataGridTextColumn Header="Ngày bắt đầu" 
                                                Binding="{Binding StartDate}" 
                                                Width="130"/>
                    <controls:DataGridTextColumn Header="Vị trí" 
                                                Binding="{Binding Position}" 
                                                Width="180"/>
                    <controls:DataGridTemplateColumn Header="Thao tác" Width="100">
                        <controls:DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                    <Button Background="Transparent" 
                        Padding="4" 
                        Click="EditEmployee_Click"
                        Tag="{Binding}">
                                        <FontIcon FontFamily="Segoe MDL2 Assets" Glyph="&#xE70F;" Foreground="#0078D7"/>
                                    </Button>
                                    <Button Background="Transparent" 
                        Padding="4" 
                        Click="DeleteEmployee_Click"
                        Tag="{Binding}">
                                        <FontIcon FontFamily="Segoe MDL2 Assets" Glyph="&#xE74D;" Foreground="#D13438"/>
                                    </Button>
                                </StackPanel>
                            </DataTemplate>
                        </controls:DataGridTemplateColumn.CellTemplate>
                    </controls:DataGridTemplateColumn>
                </controls:DataGrid.Columns>
            </controls:DataGrid>
        </Grid>
    </Grid>
</Page>