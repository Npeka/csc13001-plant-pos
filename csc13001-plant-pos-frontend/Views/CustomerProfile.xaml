<Page
    x:Class="csc13001_plant_pos_frontend.Views.CustomerProfile"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:csc13001_plant_pos_frontend.Views"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid Padding="20">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="250"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Sidebar -->
        <StackPanel Background="Black" Padding="20">
            <Image Source="Assets/user.png" Width="80" Height="80" HorizontalAlignment="Center" />
            <TextBlock Text="Roland Donald" FontSize="18" FontWeight="Bold" Foreground="White" Margin="0,10,0,2" TextAlignment="Center"/>
            <TextBlock Text="Nhân viên thu ngân" FontSize="14" Foreground="Gray" TextAlignment="Center"/>

            <ListView Margin="0,20,0,0">
                <ListViewItem Content="Thông tin cá nhân"/>
                <ListViewItem Content="Xóa tài khoản"/>
            </ListView>
        </StackPanel>

        <!-- Main Content -->
        <StackPanel Grid.Column="1" Padding="20">
            <TextBlock Text="Thông tin cá nhân" FontSize="20" FontWeight="Bold"/>

            <Grid Margin="0,10,0,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="1*"/>
                    <ColumnDefinition Width="1*"/>
                </Grid.ColumnDefinitions>

                <TextBox x:Name="FirstNameTextBox" Header="Họ" Grid.Column="0" Text="Roland" TextChanged="OnFieldChanged"/>
                <TextBox x:Name="LastNameTextBox" Header="Tên" Grid.Column="1" Margin="10,0,0,0" Text="Donald"  TextChanged="OnFieldChanged"/>
            </Grid>

            <Grid Margin="0,10,0,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="2*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBox x:Name="EmailTextBox" Header="Email" Grid.Column="0" Text="rolandDonald@gmail.com"  TextChanged="OnFieldChanged"/>
                <TextBlock Text="✔ Đã xác minh" Foreground="Green" VerticalAlignment="Bottom" Margin="10,0,0,0" Grid.Column="1"/>
            </Grid>
            <Grid Margin="0,10,0,0">
                <TextBox x:Name="AddressTextBox" Header="Địa chỉ" Margin="0,10,0,0" Text="3605 Parker Rd."  TextChanged="OnFieldChanged"/>
            </Grid>
            <Grid Margin="0,10,0,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="1*"/>
                    <ColumnDefinition Width="1*"/>
                </Grid.ColumnDefinitions>

                <TextBox x:Name="PhoneNumberTextBox" Header="Số điện thoại" Grid.Column="0" Text="(405) 555-0128"  TextChanged="OnFieldChanged"/>
                <TextBox x:Name="PostalCodeTextBox" Header="Mã bưu chính" Grid.Column="1" Margin="10,0,0,0" Text="30301"  TextChanged="OnFieldChanged"/>
            </Grid>

            <Grid Margin="0,10,0,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="1*"/>
                    <ColumnDefinition Width="1*"/>
                </Grid.ColumnDefinitions>

                <ComboBox x:Name="LocationComboBox" Header="Khu vực" Grid.Column="0">
                    <ComboBoxItem Content="Chưa biết" IsSelected="True"/>
                    <ComboBoxItem Content="Hồ Chí Minh"/>
                    <ComboBoxItem Content="Hà Nội"/>
                </ComboBox>
                <StackPanel Grid.Column="1">
                    <TextBlock Text="Ngày tạo hồ sơ" FontWeight="Bold" FontSize="14" Margin="0,0,0,5"/>
                    <TextBlock x:Name="ProfileCreatedDateText" FontSize="14" Foreground="White" Text="11/11/2025"/>
                </StackPanel>
            </Grid>

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
                <Button x:Name="DiscardButton" Content="Hủy thay đổi" Click="DiscardChanges_Click" Background="Transparent" BorderBrush="Orange" Foreground="Orange" Visibility="Collapsed"/>

                <Button Content="Lưu thay đổi" Click="SaveChanges_Click" Background="Orange" Foreground="White" Margin="10,0,0,0"/>
            </StackPanel>
        </StackPanel>
    </Grid>
</Page>
