<Page
    x:Class="csc13001_plant_pos_frontend.Views.StatisticPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:csc13001_plant_pos_frontend.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Padding="20" Background="#F8F8F8">
        <StackPanel Spacing="20">

            <!-- Header Controls -->
            <StackPanel Orientation="Horizontal" Spacing="10">
                <ComboBox Width="150" Header="Last 7 days">
                    <ComboBoxItem Content="Last 7 days" IsSelected="True"/>
                    <ComboBoxItem Content="Last 30 days"/>
                    <ComboBoxItem Content="This month"/>
                </ComboBox>
                <Button Content="Export" Padding="10,5" Background="LightGray"/>
            </StackPanel>

            <!-- Statistics Overview -->
            <Grid Margin="10">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Border Background="White" CornerRadius="10" Padding="15" Grid.Column="0">
                    <StackPanel HorizontalAlignment="Center">
                        <TextBlock Text="Revenue" FontSize="16" FontWeight="Bold"/>
                        <TextBlock Text="$14,329" FontSize="24" FontWeight="Bold"/>
                        <TextBlock Text="▼ 10%" Foreground="Red"/>
                    </StackPanel>
                </Border>

                <Border Background="White" CornerRadius="10" Padding="15" Grid.Column="1" Margin="10,0">
                    <StackPanel HorizontalAlignment="Center">
                        <TextBlock Text="Orders" FontSize="16" FontWeight="Bold"/>
                        <TextBlock Text="2,506" FontSize="24" FontWeight="Bold"/>
                        <TextBlock Text="▲ 20%" Foreground="Green"/>
                    </StackPanel>
                </Border>

                <Border Background="White" CornerRadius="10" Padding="15" Grid.Column="2">
                    <StackPanel HorizontalAlignment="Center">
                        <TextBlock Text="Average" FontSize="16" FontWeight="Bold"/>
                        <TextBlock Text="$2,047" FontSize="24" FontWeight="Bold"/>
                        <TextBlock Text="▼ 10%" Foreground="Red"/>
                    </StackPanel>
                </Border>
            </Grid>

            <!-- Sales Chart Placeholder -->
            <Border Background="White" CornerRadius="10" Padding="15" Height="200">
                <TextBlock Text="Sales Chart (Placeholder)" HorizontalAlignment="Center" VerticalAlignment="Center" Foreground="Gray"/>
            </Border>

            <!-- Sales Data Table -->
            <Border Background="White" CornerRadius="10" Padding="15" Margin="0,10">
                <ListView ItemsSource="{x:Bind SalesDataList, Mode=OneWay}">
                    <ListView.Header>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="2*"/>
                                <ColumnDefinition Width="1*"/>
                                <ColumnDefinition Width="1*"/>
                                <ColumnDefinition Width="1*"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="Name" FontWeight="Bold" Grid.Column="0"/>
                            <TextBlock Text="Sales" FontWeight="Bold" Grid.Column="1"/>
                            <TextBlock Text="Refunds" FontWeight="Bold" Grid.Column="2"/>
                            <TextBlock Text="Net" FontWeight="Bold" Grid.Column="3"/>
                        </Grid>
                    </ListView.Header>
                    <ListView.ItemTemplate>
                        <DataTemplate x:DataType="local:SalesData">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="2*"/>
                                    <ColumnDefinition Width="1*"/>
                                    <ColumnDefinition Width="1*"/>
                                    <ColumnDefinition Width="1*"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Text="{x:Bind Name}" Grid.Column="0"/>
                                <TextBlock Text="{x:Bind Sales}" Grid.Column="1"/>
                                <TextBlock Text="{x:Bind Refunds}" Grid.Column="2"/>
                                <TextBlock Text="{x:Bind Net}" Grid.Column="3"/>
                            </Grid>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </Border>
        </StackPanel>
    </Grid>
</Page>
